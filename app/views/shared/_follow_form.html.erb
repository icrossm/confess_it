<% unless current_user?(@user)%>
<div id="follow_form">
	<% if current_user.following?(@user)%>
	
	<!-- <%=  link_to "UnFollow",relationships_path(:id => @user.id), :remote => 'true', :class => "UnFollowButton_small"%>	 -->
	<%= render 'shared/unfollow'%>
	<%else%>
	<%=  link_to "Follow",relationships_path(:id => @user.id), :method => :post, :remote => 'true', :class => "FollowButton_small"%>
	<!-- <%= render 'shared/follow' %> -->
	<%end%>
	
</div>
<%end%>